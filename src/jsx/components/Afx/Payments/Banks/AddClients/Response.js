import React from 'react';
import PropTypes from 'prop-types';

const Response = props => {
    return (
        <div>

        </div>
    );
};

Response.propTypes = {
    
};

export default Response;